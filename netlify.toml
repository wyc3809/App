[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_BASE = "/"

# Existing files under /downloads/ are served as-is (force not set).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
